<%= form_with(model: vocabulary_test, local: true) do |form| %>
  <% if vocabulary_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vocabulary_test.errors.count, "error") %> prohibited this vocabulary_test from being saved:</h2>

      <ul>
      <% vocabulary_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
